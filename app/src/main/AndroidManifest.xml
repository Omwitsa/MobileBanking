<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aratek.trustfinger">

    <application
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.AuthenticationActivity"></activity>
        <activity android:name=".Activities.AccountsActivity" />
        <activity android:name=".utils.MainActivity" />
        <activity android:name=".Activities.FingeprintActivity">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_DETACHED" />
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>

            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>
        <activity android:name=".Activities.VerifyActivity" />
        <activity android:name=".Activities.PrintActivity" />
        <activity android:name=".Activities.MyListActivity" />
        <activity android:name=".Activities.WithdrawActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.ProductsActivity" />
        <activity android:name=".Activities.DepositActivity" />
        <activity android:name=".Activities.BalanceActivity" />
        <activity android:name=".Activities.AgentMemberActivity" />
        <activity android:name=".Activities.HomeActivity" />
        <activity android:name=".Activities.AdvanceActivity" />
        <activity android:name=".utils.Printer" />
        <activity android:name=".Activities.Reports" />
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />

    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="true" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

</manifest>